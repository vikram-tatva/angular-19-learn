image: node:20

cache:
  paths:
    - node_modules/

stages:
  - install
  - build
  - test

install:
  stage: install
  script:
    - npm install

build:
  stage: build
  script:
    - npm run build -- --configuration production
  artifacts:
    paths:
      - dist/

test:
  stage: test
  script:
    - npm run test -- --watch=false --browsers=ChromeHeadless
